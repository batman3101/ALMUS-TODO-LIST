{
  "buildCommand": "npm run build --workspace=@almus/shared-types && npm run build --workspace=@almus/web-app",
  "outputDirectory": "apps/web-app/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
