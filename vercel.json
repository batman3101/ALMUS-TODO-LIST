{
  "buildCommand": "npm run build --workspace=@almus/shared-types && npm run build --workspace=@almus/web-app && node scripts/copy-dist.js",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}