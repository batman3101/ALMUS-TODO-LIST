# ================================
# ALMUS Todo List - 개발환경 설정 템플릿
# ================================
# 이 파일을 .env.local로 복사하고 실제 값으로 변경하세요
# cp .env.example .env.local

# ----------------
# Supabase Configuration (필수)
# ----------------
# Supabase 프로젝트 설정에서 이 값들을 가져오세요:
# https://app.supabase.com/project/{your-project}/settings/api

# Supabase URL (예: https://your-project-id.supabase.co)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key (공개 키 - 클라이언트에서 사용)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ----------------
# App Configuration (선택사항)
# ----------------
VITE_APP_NAME=ALMUS ToDo List
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=development

# ----------------
# Development Features (선택사항)
# ----------------
# 개발 모드에서만 사용되는 기능들
VITE_ENABLE_DEV_TOOLS=true
VITE_ENABLE_MOCK_DATA=false
VITE_DEBUG_MODE=true

# ----------------
# File Upload Settings (선택사항)
# ----------------
VITE_MAX_FILE_SIZE_MB=10
VITE_ALLOWED_FILE_TYPES=pdf,doc,docx,xls,xlsx,jpg,jpeg,png,gif
VITE_MAX_FILES_COUNT=5

# ----------------
# API Configuration (선택사항)
# ----------------
VITE_API_TIMEOUT=30000
VITE_REACT_QUERY_STALE_TIME=300000

# =====================================
# 🔐 보안 주의사항:
# =====================================
# 1. .env.local 파일은 실제 API 키를 포함하므로 Git에 커밋하지 마세요
# 2. VITE_ 접두사가 있는 변수만 클라이언트에서 접근 가능합니다  
# 3. 민감한 서버 측 정보는 VITE_ 접두사를 사용하지 마세요
# 4. 팀원과 실제 키 공유 시에는 안전한 방법을 사용하세요

# =====================================
# 📝 사용 방법:
# =====================================
# 1. 이 파일을 .env.local로 복사
# 2. Supabase에서 실제 URL과 Key 발급
# 3. 위의 샘플 값들을 실제 값으로 교체
# 4. 개발 서버 재시작: npm run dev